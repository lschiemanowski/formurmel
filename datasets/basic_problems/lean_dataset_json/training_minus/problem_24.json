{
  "statement": "A finite group of prime order is cyclic.",
  "lean_statement": "theorem isCyclic_of_prime_card_fintype {G : Type*} [Group G] [Fintype G] (h : Nat.Prime (Fintype.card G)) :\n    IsCyclic G",
  "proof": "Proof: let `|G| = p` with `p` prime. Pick `g` not equal to `1`. Consider the subgroup generated by\n`g`; its\ncardinality divides `|G|` (Lagrange). It is not `1` since it contains `g`, so its order is a\ndivisor of `p` different from `1`, hence it must be `p`. Therefore this subgroup is all of `G`,\nso `g` generates `G` and `G` is cyclic."
}
