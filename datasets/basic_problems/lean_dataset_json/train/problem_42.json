{
  "statement": "A subgroup of index `2` is normal (again).",
  "lean_statement": "theorem subgroup_normal_of_index_eq_two_alt\n    {G : Type*} [Group G] (H : Subgroup G) (hH : H.index = 2) :\n    H.Normal",
  "proof": "Proof: since `H.index = 2`, there are exactly two left cosets of `H`: `H` itself and the other one.\nFor any `g`, if `g ∈ H` then `gH = H = Hg`. If `g ∉ H`, then `gH` is the unique left coset\ndifferent from `H`, and `Hg` is the unique right coset different from `H`, so necessarily `gH = Hg`.\nThus `gH = Hg` for all `g`, which is exactly that `H` is normal."
}
